"use client";

import { useEffect, useMemo, useState } from "react";
import { createClient } from "@/lib/supabase/client";
import { AdminPageShell } from "@/components/admin/AdminPageShell";

type BoolMap=Record<string,boolean>;type Role={id:string;role_name:string;role_category:string;permissions:Record<string,unknown>;is_system:boolean;is_active:boolean};type Inventory={view:boolean;add:boolean;edit:boolean;remove:boolean;sr_approval:boolean};type ItemSr={view:boolean;create:boolean;history:boolean};
const categories=["Administration","Finance & Accounts","Academic","Human Resources","Student / Parent","Inventory & Store","Procurement","Library","Transport","Laboratory","Operations","Other Members"];
const legacy=[["dashboard","Dashboard"],["profile","Profile"],["attendance","Attendance"],["students","Students"],["parents","Parents & Guardians"],["people","People & Achievements"],["teachers","Teachers & Staff"],["store_members","Store Members"],["notices","Notices"],["results","Results & Reports"]] as const;
const parentList=[["dashboard","Dashboard"],["profile","My Profile"],["parents","My Children / Registration Status"],["notices","Notices"],["results","Results"],["attendance","Attendance"],["fees","Fees & Payment Status"],["routine","Class Routine"],["exam_schedule","Exam Schedule"],["documents","Documents"],["transport","Transport"],["notifications","Notifications"]] as const;
const studentList=[["dashboard","Dashboard"],["profile","My Profile"],["notices","Notices"],["results","My Results"],["attendance","My Attendance"],["fees","My Fees"],["routine","Class Routine"],["exam_schedule","Exam Schedule"],["documents","My Documents"],["library","Library"],["transport","Transport"],["notifications","Notifications"]] as const;
const inventoryList=[["view","View / Search Items"],["add","Add New Item"],["edit","Edit Item"],["remove","Remove Item"],["sr_approval","SR Approval"]] as const;const itemSrList=[["view","View / Search Items"],["create","Create SR"],["history","View Own SR History"]] as const;const salaryList=[["salary_view","View Salary Sheets"],["salary_status","View Salary Status"],["salary_approval","Approve Salary"],["salary_payment","Make Salary Payment"],["salary_history","View Salary History"]] as const;const billList=[["bill_view","View Bill Payments"],["bill_create","Create Bill"],["bill_edit","Edit Bill"],["bill_approve","Approve Bill"],["bill_payment","Make Payment"],["bill_history","View Payment History"],["bill_cancel","Cancel / Void Bill"],["bill_reports","View Reports"]] as const;
const empty=(keys:readonly string[]):BoolMap=>Object.fromEntries(keys.map(k=>[k,false]));const emptyInventory=():Inventory=>({view:false,add:false,edit:false,remove:false,sr_approval:false});const emptyItemSr=():ItemSr=>({view:false,create:false,history:false});
export default function AdminRolesPage(){const supabase=useMemo(()=>createClient(),[]);const [roles,setRoles]=useState<Role[]>([]);const [roleName,setRoleName]=useState("");const [roleCategory,setRoleCategory]=useState("Other Members");const [editingId,setEditingId]=useState<string|null>(null);const [inventory,setInventory]=useState<Inventory>(emptyInventory());const [itemSr,setItemSr]=useState<ItemSr>(emptyItemSr());const [salary,setSalary]=useState<BoolMap>(()=>empty(salaryList.map(x=>x[0])));const [bill,setBill]=useState<BoolMap>(()=>empty(billList.map(x=>x[0])));const [other,setOther]=useState<BoolMap>(()=>empty(legacy.map(x=>x[0])));const [parentPerm,setParentPerm]=useState<BoolMap>(()=>empty(parentList.map(x=>x[0])));const [studentPerm,setStudentPerm]=useState<BoolMap>(()=>empty(studentList.map(x=>x[0])));const [loading,setLoading]=useState(true);const [saving,setSaving]=useState(false);const [message,setMessage]=useState("");const [error,setError]=useState("");
async function loadRoles(){setLoading(true);const {data,error}=await supabase.rpc("store_admin_list_member_roles");if(error)setError(error.message);else setRoles((data??[]) as Role[]);setLoading(false);}useEffect(()=>{void loadRoles()},[]);
function reset(){setEditingId(null);setRoleName("");setRoleCategory("Other Members");setInventory(emptyInventory());setItemSr(emptyItemSr());setSalary(empty(salaryList.map(x=>x[0])));setBill(empty(billList.map(x=>x[0])));setOther(empty(legacy.map(x=>x[0])));setParentPerm(empty(parentList.map(x=>x[0])));setStudentPerm(empty(studentList.map(x=>x[0])));}
function editRole(role:Role){const inv=role.permissions?.inventory&&typeof role.permissions.inventory==="object"?role.permissions.inventory as Partial<Inventory>:{};const sr=role.permissions?.item_sr&&typeof role.permissions.item_sr==="object"?role.permissions.item_sr as Partial<ItemSr>:{};const acc=role.permissions?.accounts&&typeof role.permissions.accounts==="object"?role.permissions.accounts as BoolMap:{};setEditingId(role.id);setRoleName(role.role_name);setRoleCategory(role.role_category||"Other Members");setInventory({...emptyInventory(),...inv});setItemSr({...emptyItemSr(),...sr});setSalary({...empty(salaryList.map(x=>x[0])),...acc});setBill({...empty(billList.map(x=>x[0])),...acc});setOther(Object.fromEntries(legacy.map(([key])=>[key,Boolean(role.permissions?.[key])] )));setParentPerm({...empty(parentList.map(x=>x[0])),...(role.permissions?.parent_portal as BoolMap||{})});setStudentPerm({...empty(studentList.map(x=>x[0])),...(role.permissions?.student_portal as BoolMap||{})});setMessage("");setError("");window.scrollTo({top:0,behavior:"smooth"});}
function toggleAll(value:boolean){setInventory({view:value,add:value,edit:value,remove:value,sr_approval:value});setItemSr({view:value,create:value,history:value});setSalary(Object.fromEntries(salaryList.map(([key])=>[key,value])));setBill(Object.fromEntries(billList.map(([key])=>[key,value])));setOther(Object.fromEntries(legacy.map(([key])=>[key,value])));setParentPerm(Object.fromEntries(parentList.map(([key])=>[key,value])));setStudentPerm(Object.fromEntries(studentList.map(([key])=>[key,value])));}
async function save(){setSaving(true);setError("");const permissions={...other,inventory,item_sr:itemSr,accounts:{...salary,...bill},parent_portal:parentPerm,student_portal:studentPerm};const {error}=await supabase.rpc("store_admin_save_member_role",{p_id:editingId,p_role_name:roleName,p_permissions:permissions,p_role_category:roleCategory});if(error)setError(error.message);else{setMessage(editingId?"Role updated successfully.":"Role created successfully.");reset();await loadRoles();}setSaving(false);}
async function remove(role:Role){if(role.is_system){setError("System roles cannot be removed.");return;}if(!window.confirm(`Remove role “${role.role_name}”?`))return;const {error}=await supabase.rpc("store_admin_remove_member_role",{p_id:role.id});if(error)setError(error.message);else{setMessage(`${role.role_name} removed.`);await loadRoles();}}
const rows=(title:string,desc:string,entries:readonly(readonly[string,string])[],values:BoolMap,setValues:React.Dispatch<React.SetStateAction<BoolMap>>,count:string)=><div className="mt-5 rounded-2xl border border-[var(--school-border)] bg-[var(--school-primary-soft)] p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-black uppercase tracking-[0.14em] theme-primary">Permission Category</p><h3 className="mt-1 text-base font-black">{title}</h3><p className="mt-0.5 text-xs text-[var(--school-muted)]">{desc}</p></div><span className="rounded-full border border-[var(--school-border)] bg-[var(--school-surface)] px-2.5 py-1 text-[10px] font-black">{count}</span></div><div className="mt-3 grid gap-2 sm:grid-cols-2">{entries.map(([key,label])=><label key={key} className="flex cursor-pointer items-center gap-3 rounded-xl border border-[var(--school-border)] bg-[var(--school-surface)] p-3"><input type="checkbox" checked={Boolean(values[key])} onChange={()=>setValues(c=>({...c,[key]:!c[key]}))} className="h-4 w-4"/><span className="min-w-0 flex-1 text-sm font-bold">{label}</span><span className={`rounded-full px-2 py-1 text-[10px] font-black ${values[key]?"theme-primary-bg":"border border-[var(--school-border)] text-[var(--school-muted)]"}`}>{values[key]?"ON":"OFF"}</span></label>)}</div></div>;
const inventoryValues=Object.fromEntries(inventoryList.map(([key])=>[key,inventory[key as keyof Inventory]]));const itemSrValues=Object.fromEntries(itemSrList.map(([key])=>[key,itemSr[key as keyof ItemSr]]));const setInventoryMap:React.Dispatch<React.SetStateAction<BoolMap>>=updater=>setInventory(current=>{const map=Object.fromEntries(inventoryList.map(([key])=>[key,current[key as keyof Inventory]]));const next=typeof updater==="function"?updater(map):updater;return {...current,...next};});const setItemSrMap:React.Dispatch<React.SetStateAction<BoolMap>>=updater=>setItemSr(current=>{const map=Object.fromEntries(itemSrList.map(([key])=>[key,current[key as keyof ItemSr]]));const next=typeof updater==="function"?updater(map):updater;return {...current,...next};});
return <AdminPageShell eyebrow="Access Control" title="Role Management" description="Roles are category-based, but every module remains granular. Admin controls the distribution of access without hard-coding modules to a user type.">{error&&<p className="mb-5 rounded-2xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</p>}{message&&<p className="mb-5 rounded-2xl border border-[var(--school-primary-border)] bg-[var(--school-primary-soft)] px-4 py-3 text-sm theme-primary">{message}</p>}<div className="grid gap-5 xl:grid-cols-[minmax(0,1.08fr)_minmax(360px,.92fr)]"><section className="rounded-3xl border border-[var(--school-border)] bg-[var(--school-surface)] p-5 shadow-sm sm:p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-black uppercase tracking-[0.16em] theme-primary">Permission Builder</p><h2 className="mt-1 text-xl font-black">{editingId?"Edit Role":"Create New Role"}</h2></div>{editingId&&<button type="button" onClick={reset} className="rounded-xl border border-[var(--school-border)] px-3 py-2 text-xs font-bold">Cancel</button>}</div><div className="mt-5 grid gap-4 md:grid-cols-2"><label><span className="text-xs font-bold text-[var(--school-muted)]">Role Name *</span><input className="mt-1 w-full rounded-xl border border-[var(--school-border)] px-3 py-2.5 text-sm" value={roleName} onChange={e=>setRoleName(e.target.value)} placeholder="e.g. Parent Coordinator" /></label><label><span className="text-xs font-bold text-[var(--school-muted)]">Role Category</span><select value={roleCategory} onChange={e=>setRoleCategory(e.target.value)} className="mt-1 w-full rounded-xl border border-[var(--school-border)] px-3 py-2.5 text-sm">{categories.map(c=><option key={c}>{c}</option>)}</select></label></div><div className="mt-4 flex flex-wrap gap-2"><button type="button" onClick={()=>toggleAll(true)} className="rounded-xl border border-[var(--school-border)] px-3 py-2 text-xs font-bold">Enable All</button><button type="button" onClick={()=>toggleAll(false)} className="rounded-xl border border-[var(--school-border)] px-3 py-2 text-xs font-bold">Disable All</button></div>{rows("Parent Portal","What a Parent can see and do in the portal.",parentList,parentPerm,setParentPerm,`${Object.values(parentPerm).filter(Boolean).length}/${parentList.length}`)}{rows("Student Portal","What a Student can see and do in the portal.",studentList,studentPerm,setStudentPerm,`${Object.values(studentPerm).filter(Boolean).length}/${studentList.length}`)}{rows("Inventory", "Items, stock and SR processing.", inventoryList, inventoryValues, setInventoryMap, `${Object.values(inventory).filter(Boolean).length}/5`)}{rows("Item SR","Member-side Service Request permissions.",itemSrList,itemSrValues,setItemSrMap,`${Object.values(itemSr).filter(Boolean).length}/3`)}{rows("Accounts • Salary","Salary access remains independent from Bill Payments.",salaryList,salary,setSalary,`${Object.values(salary).filter(Boolean).length}/5`)}{rows("Accounts • Bill Payments","View, create, edit, approve, pay, history, cancel and report permissions.",billList,bill,setBill,`${Object.values(bill).filter(Boolean).length}/8`)}{rows("Other Permissions","Existing non-Accounts permissions remain independent.",legacy,other,setOther,`${Object.values(other).filter(Boolean).length}/${legacy.length}`)}<button type="button" disabled={saving||!roleName.trim()} onClick={()=>void save()} className="mt-5 w-full rounded-xl px-5 py-3.5 text-sm font-bold theme-primary-bg disabled:opacity-60">{saving?"Saving...":editingId?"Update Role":"Create Role"}</button></section><section className="rounded-3xl border border-[var(--school-border)] bg-[var(--school-surface)] p-5 shadow-sm sm:p-6"><p className="text-[10px] font-black uppercase tracking-[0.16em] theme-primary">Role Library</p><h2 className="mt-1 text-xl font-black">Available Roles</h2><div className="mt-5 space-y-3">{loading?<p className="rounded-2xl border border-dashed border-[var(--school-border)] p-8 text-center text-sm text-[var(--school-muted)]">Loading roles...</p>:roles.map(role=><article key={role.id} className="rounded-2xl border border-[var(--school-border)] p-4"><div className="flex items-start justify-between gap-3"><div><h3 className="text-sm font-black">{role.role_name}</h3><p className="mt-1 text-[10px] text-[var(--school-muted)]">{role.role_category||"Other Members"}</p></div><div className="flex gap-2"><button type="button" onClick={()=>editRole(role)} className="rounded-xl border border-[var(--school-border)] px-3 py-2 text-xs font-bold">Edit</button>{!role.is_system&&<button type="button" onClick={()=>void remove(role)} className="rounded-xl border border-red-200 px-3 py-2 text-xs font-bold text-red-600">Remove</button>}</div></div></article>)}</div></section></div></AdminPageShell>;
}
