"use client";
import { useEffect,useMemo,useState } from "react";
import { useRouter } from "next/navigation";
import { createClient } from "@/lib/supabase/client";
import ItemSrModule from "./item-sr";
import MySrList from "./my-sr-list";
import InventoryModule from "./inventory";
import AccountsModule from "./accounts";
import MemberAccountsV2 from "./member-accounts-v2";
import ParentPortal from "../parent-portal";
import StudentPanel from "../student-panel";
type InventoryPermissions={view:boolean;add:boolean;edit:boolean;remove:boolean;sr_approval:boolean};type ItemSrPermissions={view:boolean;create:boolean;history:boolean};type CurrentUser={member_id:string;member_type:string;access_role:string;role_name:string;permissions:Record<string,unknown>;full_name:string;photo_url:string|null;email:string|null;phone:string|null;designation:string|null;department:string|null;subject:string|null};type ModuleKey="dashboard"|"profile"|"item_sr"|"attendance"|"students"|"parents"|"people"|"teachers"|"store_members"|"inventory"|"accounts"|"notices"|"results";type ModuleDefinition={key:ModuleKey;label:string};
const modules:ModuleDefinition[]=[{key:"dashboard",label:"Dashboard"},{key:"profile",label:"Profile"},{key:"item_sr",label:"Item SR"},{key:"attendance",label:"Attendance"},{key:"students",label:"Students"},{key:"parents",label:"Parents & Guardians"},{key:"people",label:"People & Achievements"},{key:"teachers",label:"Teachers & Staff"},{key:"store_members",label:"Store Members"},{key:"inventory",label:"Inventory"},{key:"accounts",label:"Accounts"},{key:"notices",label:"Notices"},{key:"results",label:"Results & Reports"}];const permissionKey:Partial<Record<ModuleKey,string>>={attendance:"attendance",students:"students",parents:"parents",people:"people",teachers:"teachers",store_members:"store_members",notices:"notices",results:"results"};
type ParentData={parent_id:string;full_name:string;phone:string|null;email:string|null;children:{id:string;registration_no:string;source:string;student_id:string|null;student_name:string;class:string;section:string|null;status:string;created_at:string;student:any}[]};
export default function MemberDashboardPage(){const router=useRouter();const supabase=useMemo(()=>createClient(),[]);const [user,setUser]=useState<CurrentUser|null>(null);const [parentData,setParentData]=useState<ParentData|null>(null);const [activeModule,setActiveModule]=useState<ModuleKey>("dashboard");const [loading,setLoading]=useState(true);const [error,setError]=useState("");
async function loadUser(){const token=window.localStorage.getItem("ctms_store_token");if(!token){router.replace("/loginportal");return;}const {data,error:userError}=await supabase.rpc("store_get_current_user",{p_token:token});if(userError){window.localStorage.removeItem("ctms_store_token");setError(userError.message);router.replace("/loginportal");return;}setUser(data as CurrentUser);if((data as CurrentUser).member_type==="parent"){const {data:pd,error:pe}=await supabase.rpc("parent_get_dashboard",{p_token:token});if(pe)setError(pe.message);else setParentData(pd as ParentData);}setLoading(false)}
useEffect(()=>{void loadUser()},[router,supabase]);async function logout(){const token=window.localStorage.getItem("ctms_store_token");if(token)await supabase.rpc("store_logout",{p_token:token});window.localStorage.removeItem("ctms_store_token");router.replace("/loginportal")}
if(loading)return <main className="min-h-screen bg-[var(--school-background)] p-6"><div className="mx-auto max-w-6xl rounded-3xl border border-[var(--school-border)] bg-[var(--school-surface)] p-10 text-center text-sm text-[var(--school-muted)]">Loading your dashboard...</div></main>;if(!user)return <main className="min-h-screen bg-[var(--school-background)] p-6"><div className="mx-auto max-w-6xl rounded-3xl border border-red-200 bg-red-50 p-10 text-center text-sm text-red-700">{error||"Unable to load your account."}</div></main>;
const inventory=(user.permissions?.inventory&&typeof user.permissions.inventory==="object"?user.permissions.inventory:{}) as Partial<InventoryPermissions>;const itemSr=(user.permissions?.item_sr&&typeof user.permissions.item_sr==="object"?user.permissions.item_sr:{}) as Partial<ItemSrPermissions>;const accounts=(user.permissions?.accounts&&typeof user.permissions.accounts==="object"?user.permissions.accounts:{}) as Record<string,boolean>;const parentPortal=(user.permissions?.parent_portal&&typeof user.permissions.parent_portal==="object"?user.permissions.parent_portal:{}) as Record<string,boolean>;const itemSrAllowed=Boolean(itemSr.view||itemSr.create||itemSr.history);const inventoryAllowed=Boolean(inventory.view||inventory.add||inventory.edit||inventory.remove||inventory.sr_approval);const accountsAllowed=Boolean(accounts.salary_view||accounts.salary_approval||accounts.salary_payment||accounts.bill_view||accounts.bill_create||accounts.bill_edit||accounts.bill_approve||accounts.bill_payment||accounts.bill_history||accounts.bill_cancel||accounts.bill_reports||accounts.student_income_view||accounts.student_income_charge||accounts.student_income_collect||accounts.student_income_edit||accounts.student_income_reports);const salaryStatusAllowed=Boolean(accounts.salary_status);const parentDashboardAllowed=Boolean(parentPortal.dashboard??user.permissions?.dashboard);const parentChildrenAllowed=Boolean(parentPortal.parents??user.permissions?.parents);
const canAccess=(key:ModuleKey)=>{if(user.member_type==="parent"&&key==="parents")return parentChildrenAllowed;if(key==="inventory")return inventoryAllowed;if(key==="item_sr")return itemSrAllowed;if(key==="accounts")return accountsAllowed;if(key==="dashboard"||key==="profile")return Boolean(user.permissions?.[key]);const keyName=permissionKey[key];return keyName?Boolean(user.permissions?.[keyName]):false};
const availableModules=modules.filter(m=>canAccess(m.key));const effectiveActiveModule=canAccess(activeModule)?activeModule:(availableModules[0]?.key??"dashboard");const activeLabel=modules.find(m=>m.key===effectiveActiveModule)?.label??"Dashboard";
return <main className="min-h-screen bg-[var(--school-background)] px-3 py-4 sm:px-5 sm:py-6 lg:px-8"><div className="mx-auto max-w-6xl"><header className="overflow-hidden rounded-3xl border border-[var(--school-border)] bg-[var(--school-surface)] shadow-sm"><div className="flex flex-col gap-4 px-5 py-4 sm:px-6 lg:flex-row lg:items-center lg:justify-between"><div className="flex min-w-0 items-center gap-3"><div className="grid h-12 w-12 shrink-0 place-items-center overflow-hidden rounded-2xl bg-[var(--school-primary-soft)] font-black theme-primary">{user.photo_url?<img src={user.photo_url} alt="" className="h-full w-full object-cover"/>:"CT"}</div><div className="min-w-0"><p className="text-[10px] font-black uppercase tracking-[0.16em] theme-primary">C.T. Model School</p><h1 className="mt-0.5 truncate text-lg font-black text-[var(--school-text)]">Welcome, {user.full_name}</h1><p className="mt-0.5 text-[11px] text-[var(--school-muted)]">{user.member_id}</p></div></div><button type="button" onClick={()=>void logout()} className="w-full rounded-xl border border-[var(--school-border)] px-4 py-2.5 text-xs font-bold hover:bg-[var(--school-primary-soft)] lg:w-auto">Logout</button></div><nav className="flex gap-1 overflow-x-auto border-t border-[var(--school-border)] px-3 py-2 sm:px-5" aria-label="Member portal navigation">{availableModules.map(module=><button key={module.key} type="button" onClick={()=>canAccess(module.key)&&setActiveModule(module.key)} className={`shrink-0 rounded-xl px-4 py-2.5 text-xs font-bold transition-colors ${effectiveActiveModule===module.key?"theme-primary-bg":"text-[var(--school-muted)] hover:bg-[var(--school-primary-soft)] hover:text-[var(--school-text)]"}`}>{module.label}</button>)}</nav></header><section className="mt-5 rounded-3xl border border-[var(--school-border)] bg-[var(--school-surface)] p-5 shadow-sm sm:p-7"><div className="flex flex-col gap-1 border-b border-[var(--school-border)] pb-5"><p className="text-[10px] font-black uppercase tracking-[0.16em] theme-primary">{activeLabel}</p><h2 className="text-2xl font-black text-[var(--school-text)]">{effectiveActiveModule==="dashboard"?`Welcome back, ${user.full_name}`:activeLabel}</h2></div>{user.member_type==="parent"&&parentData&&parentDashboardAllowed&&effectiveActiveModule==="dashboard"&&<div className="mt-6 space-y-6"><StudentPanel children={parentData.children}/><ParentPortal parent={parentData} onRefresh={()=>void loadUser()}/></div>}{user.member_type==="parent"&&parentData&&parentChildrenAllowed&&effectiveActiveModule==="parents"&&<div className="mt-6 space-y-6"><StudentPanel children={parentData.children}/><ParentPortal parent={parentData} onRefresh={()=>void loadUser()}/></div>}{effectiveActiveModule==="dashboard"&&user.member_type!=="parent"&&<><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><div className="rounded-2xl border border-[var(--school-border)] bg-[var(--school-primary-soft)] p-5"><p className="text-xs font-bold text-[var(--school-muted)]">Member ID</p><p className="mt-2 text-lg font-black theme-primary">{user.member_id}</p></div><div className="rounded-2xl border border-[var(--school-border)] bg-[var(--school-primary-soft)] p-5"><p className="text-xs font-bold text-[var(--school-muted)]">Designation</p><p className="mt-2 text-lg font-black text-[var(--school-text)]">{user.designation||"-"}</p></div><div className="rounded-2xl border border-[var(--school-border)] bg-[var(--school-primary-soft)] p-5"><p className="text-xs font-bold text-[var(--school-muted)]">Department</p><p className="mt-2 text-lg font-black text-[var(--school-text)]">{user.department||"-"}</p></div></div>{salaryStatusAllowed&&<MemberAccountsV2 compact/>}</>}{effectiveActiveModule==="profile"&&<><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{[["Member ID",user.member_id],["Full Name",user.full_name],["Designation",user.designation||"-"],["Department",user.department||"-"],["Subject",user.subject||"-"],["Phone",user.phone||"-"],["Email",user.email||"-"]].map(([label,value])=><div key={label} className="rounded-2xl border border-[var(--school-border)] p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-[var(--school-muted)]">{label}</p><p className="mt-1 break-words text-sm font-bold text-[var(--school-text)]">{value}</p></div>)}</div>{salaryStatusAllowed&&<MemberAccountsV2/>}</>}{effectiveActiveModule==="item_sr"&&<><ItemSrModule department={user.department} permissions={itemSr}/><MySrList canView={Boolean(itemSr.history)}/></>}{effectiveActiveModule==="inventory"&&<InventoryModule permissions={inventory}/>} {effectiveActiveModule==="accounts"&&<AccountsModule permissions={accounts}/>} {effectiveActiveModule!=="dashboard"&&effectiveActiveModule!=="profile"&&effectiveActiveModule!=="item_sr"&&effectiveActiveModule!=="inventory"&&effectiveActiveModule!=="accounts"&&!(user.member_type==="parent"&&effectiveActiveModule==="parents")&&<div className="mt-6 rounded-2xl border border-dashed border-[var(--school-border)] bg-[var(--school-primary-soft)] p-8 text-center"><h3 className="text-base font-black text-[var(--school-text)]">{activeLabel}</h3><p className="mt-2 text-sm text-[var(--school-muted)]">Access is enabled for your role. This section is ready for its module features.</p></div>}</section></div></main>}
